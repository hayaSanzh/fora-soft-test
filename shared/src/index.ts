/**
 * Публичная точка входа общего пакета (TDD §2.2).
 *
 * Здесь живёт только то, что обязано совпадать у клиента и сервера.
 * Контракт событий Socket.io, типы данных и zod-схемы добавляются группой 2
 * (`events.ts`, `validation.ts`) и реэкспортируются отсюда же.
 */

export * from './protocol.js';

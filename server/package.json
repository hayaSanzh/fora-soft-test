{
  "name": "@video-chat/server",
  "version": "0.1.0",
  "private": true,
  "description": "Сигнальный сервер: express (статика + /health) и socket.io",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@video-chat/shared": "*",
    "express": "^4.21.2",
    "nanoid": "^5.1.16",
    "pino": "^9.6.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
